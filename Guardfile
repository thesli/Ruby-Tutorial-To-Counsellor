# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard :rspec,:cli => "-fh -o test.html" do  
  watch("hello_vava.rb")     { 
    "spec/hello_vava.spec.rb"    
   }
end

# guard "livereload" do
#   watch ("test.html")
# end
